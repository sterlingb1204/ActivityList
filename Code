import java.util.*;

Public class Activity
{
  Public static void main(String [] args)
   {
      Scanner Activity = new Scanner(new File("Date.txt"));
      ArrayList<String> list = new ArrayList<String>();
      while (Activity.hasNext())
        list.add(Activity.next());
        while(file.hasNext()){
                String a = file.nextLine();
                //Collections.shuffle(words);
                //String pickWord = words.get(1);
                //String[] a = 
                System.out.println(a);   
     
     }
   int index = randomGenerator.nextLine(catalogue.size());
        return catalogue.get(index);
        System.out.println("The choice is...." + anyItem + "like it or lump it.");
        System.out.println("Do you wish to lump it? ");
        char y = new Scanner;
        if(y)
    }
    while(file.hasNext()){
                String a = file.nextLine();
                //Collections.shuffle(words);
                //String pickWord = words.get(1);
                //String[] a = 
                System.out.println(a);
}
Activity.close();
